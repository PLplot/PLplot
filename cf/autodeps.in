# -*-makefile-*----------------------------------------------------------------
# $Id$
#
# Pull in the dependencies.
# -----------------------------------------------------------------------------

ifeq ($(automatic_deps),yes)
-include .d.* shared/.d.*
else
include objs.in
endif

